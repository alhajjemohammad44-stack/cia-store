// NextAuth API route placeholder
// In production, this handles auth requests

import { NextResponse } from 'next/server'

export const GET = () => NextResponse.json({ message: 'Auth API - configure NextAuth in production' })
export const POST = () => NextResponse.json({ message: 'Auth API - configure NextAuth in production' })
